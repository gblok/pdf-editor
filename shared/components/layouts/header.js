const save = () => {

    // const doc = parseQuill(this.quill.getContents())
    // LOG({doc})
    //
    // getBlobUrl(doc).then(href => this.setState({href}))

}


export default ({title}) => <header><logo>{title}</logo></header>


//     < actions >
//     < button
// onClick = {save} > Build
// PDF < /button>
// </actions>
// {
//     href
//         ? <a href={href} download>Download PDF</a>
//         : null
//
// }